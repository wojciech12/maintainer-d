apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  creationTimestamp: null
  name: v251229-074b474.collaborators.maintainer-d.cncf.io
spec:
  group: maintainer-d.cncf.io
  names:
    categories:
    - maintainerd
    kind: Collaborator
    listKind: CollaboratorList
    plural: collaborators
    shortNames:
    - collab
    singular: collaborator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      description: Collaborator represents a contributor with limited responsibilities.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: CollaboratorSpec captures collaborator specific attributes.
          properties:
            displayName:
              type: string
            externalIDs:
              additionalProperties:
                type: string
              type: object
            gitHubAccount:
              type: string
            gitHubEmail:
              type: string
            lastLogin:
              format: date-time
              type: string
            primaryEmail:
              type: string
            projects:
              items:
                description: |-
                  ResourceReference expresses a loose reference to another resource. The UID is optional
                  but helps controllers confirm identity when present.
                properties:
                  name:
                    description: Name is the resource name within its workspace.
                    type: string
                  uid:
                    description: UID is set by controllers that want to pin to a specific
                      resource instance.
                    type: string
                  workspace:
                    description: Workspace optionally identifies the kcp workspace
                      that owns the target resource.
                    type: string
                required:
                - name
                type: object
              type: array
            registeredAt:
              format: date-time
              type: string
          required:
          - displayName
          type: object
        status:
          description: CollaboratorStatus reports derived collaborator information.
          properties:
            lastSynced:
              format: date-time
              type: string
            observedProjects:
              items:
                description: ProjectReference embeds additional project specific metadata
                  for status views.
                properties:
                  name:
                    description: Name is the resource name within its workspace.
                    type: string
                  roles:
                    description: Roles reflects the project roles this subject currently
                      holds.
                    items:
                      type: string
                    type: array
                  uid:
                    description: UID is set by controllers that want to pin to a specific
                      resource instance.
                    type: string
                  workspace:
                    description: Workspace optionally identifies the kcp workspace
                      that owns the target resource.
                    type: string
                required:
                - name
                type: object
              type: array
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}
