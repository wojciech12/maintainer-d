apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  creationTimestamp: null
  name: v251229-074b474.projects.maintainer-d.cncf.io
spec:
  group: maintainer-d.cncf.io
  names:
    categories:
    - maintainerd
    kind: Project
    listKind: ProjectList
    plural: projects
    shortNames:
    - proj
    singular: project
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      description: Project represents a CNCF project or subproject.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: ProjectSpec captures desired project configuration.
          properties:
            collaboratorRefs:
              items:
                description: |-
                  ResourceReference expresses a loose reference to another resource. The UID is optional
                  but helps controllers confirm identity when present.
                properties:
                  name:
                    description: Name is the resource name within its workspace.
                    type: string
                  uid:
                    description: UID is set by controllers that want to pin to a specific
                      resource instance.
                    type: string
                  workspace:
                    description: Workspace optionally identifies the kcp workspace
                      that owns the target resource.
                    type: string
                required:
                - name
                type: object
              type: array
            displayName:
              type: string
            foundationRef:
              description: |-
                ResourceReference expresses a loose reference to another resource. The UID is optional
                but helps controllers confirm identity when present.
              properties:
                name:
                  description: Name is the resource name within its workspace.
                  type: string
                uid:
                  description: UID is set by controllers that want to pin to a specific
                    resource instance.
                  type: string
                workspace:
                  description: Workspace optionally identifies the kcp workspace that
                    owns the target resource.
                  type: string
              required:
              - name
              type: object
            mailingList:
              type: string
            maintainerLeadRef:
              description: |-
                ResourceReference expresses a loose reference to another resource. The UID is optional
                but helps controllers confirm identity when present.
              properties:
                name:
                  description: Name is the resource name within its workspace.
                  type: string
                uid:
                  description: UID is set by controllers that want to pin to a specific
                    resource instance.
                  type: string
                workspace:
                  description: Workspace optionally identifies the kcp workspace that
                    owns the target resource.
                  type: string
              required:
              - name
              type: object
            maintainerRefs:
              items:
                description: |-
                  ResourceReference expresses a loose reference to another resource. The UID is optional
                  but helps controllers confirm identity when present.
                properties:
                  name:
                    description: Name is the resource name within its workspace.
                    type: string
                  uid:
                    description: UID is set by controllers that want to pin to a specific
                      resource instance.
                    type: string
                  workspace:
                    description: Workspace optionally identifies the kcp workspace
                      that owns the target resource.
                    type: string
                required:
                - name
                type: object
              type: array
            maturity:
              description: ProjectMaturity enumerates the CNCF maturity stages.
              type: string
            onboardingIssue:
              type: string
            parentProjectRef:
              description: |-
                ResourceReference expresses a loose reference to another resource. The UID is optional
                but helps controllers confirm identity when present.
              properties:
                name:
                  description: Name is the resource name within its workspace.
                  type: string
                uid:
                  description: UID is set by controllers that want to pin to a specific
                    resource instance.
                  type: string
                workspace:
                  description: Workspace optionally identifies the kcp workspace that
                    owns the target resource.
                  type: string
              required:
              - name
              type: object
            serviceRefs:
              items:
                description: |-
                  ResourceReference expresses a loose reference to another resource. The UID is optional
                  but helps controllers confirm identity when present.
                properties:
                  name:
                    description: Name is the resource name within its workspace.
                    type: string
                  uid:
                    description: UID is set by controllers that want to pin to a specific
                      resource instance.
                    type: string
                  workspace:
                    description: Workspace optionally identifies the kcp workspace
                      that owns the target resource.
                    type: string
                required:
                - name
                type: object
              type: array
            tags:
              additionalProperties:
                type: string
              type: object
          required:
          - displayName
          type: object
        status:
          description: ProjectStatus reports reconciliation signals for a project.
          properties:
            collaboratorCount:
              type: integer
            conditions:
              items:
                description: Condition contains details for one aspect of the current
                  state of this API Resource.
                properties:
                  lastTransitionTime:
                    description: |-
                      lastTransitionTime is the last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      message is a human readable message indicating details about the transition.
                      This may be an empty string.
                    maxLength: 32768
                    type: string
                  observedGeneration:
                    description: |-
                      observedGeneration represents the .metadata.generation that the condition was set based upon.
                      For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                      with respect to the current state of the instance.
                    format: int64
                    minimum: 0
                    type: integer
                  reason:
                    description: |-
                      reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      Producers of specific condition types may define expected values and meanings for this field,
                      and whether the values are considered a guaranteed API.
                      The value should be a CamelCase string.
                      This field may not be empty.
                    maxLength: 1024
                    minLength: 1
                    pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                    type: string
                  status:
                    description: status of the condition, one of True, False, Unknown.
                    enum:
                    - "True"
                    - "False"
                    - Unknown
                    type: string
                  type:
                    description: type of condition in CamelCase or in foo.example.com/CamelCase.
                    maxLength: 316
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                    type: string
                required:
                - lastTransitionTime
                - message
                - reason
                - status
                - type
                type: object
              type: array
            lastSynced:
              format: date-time
              type: string
            maintainerCount:
              type: integer
            serviceCount:
              type: integer
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}
