---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: maintainers.maintainer-d.cncf.io
spec:
  group: maintainer-d.cncf.io
  names:
    categories:
    - maintainerd
    kind: Maintainer
    listKind: MaintainerList
    plural: maintainers
    shortNames:
    - maint
    singular: maintainer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Maintainer represents an individual with project responsibilities.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MaintainerSpec captures desired maintainer attributes.
            properties:
              companyRef:
                description: |-
                  ResourceReference expresses a loose reference to another resource. The UID is optional
                  but helps controllers confirm identity when present.
                properties:
                  name:
                    description: Name is the resource name within its workspace.
                    type: string
                  uid:
                    description: UID is set by controllers that want to pin to a specific
                      resource instance.
                    type: string
                  workspace:
                    description: Workspace optionally identifies the kcp workspace
                      that owns the target resource.
                    type: string
                required:
                - name
                type: object
              displayName:
                type: string
              externalIDs:
                additionalProperties:
                  type: string
                type: object
              gitHubAccount:
                type: string
              gitHubEmail:
                type: string
              primaryEmail:
                type: string
              registeredAt:
                format: date-time
                type: string
              status:
                description: MaintainerLifecycle enumerates lifecycle states for a
                  maintainer.
                type: string
            required:
            - displayName
            - status
            type: object
          status:
            description: MaintainerStatus surfaces derived information gathered by
              controllers.
            properties:
              importWarnings:
                items:
                  type: string
                type: array
              lastSynced:
                format: date-time
                type: string
              projectMemberships:
                items:
                  description: ProjectReference embeds additional project specific
                    metadata for status views.
                  properties:
                    name:
                      description: Name is the resource name within its workspace.
                      type: string
                    roles:
                      description: Roles reflects the project roles this subject currently
                        holds.
                      items:
                        type: string
                      type: array
                    uid:
                      description: UID is set by controllers that want to pin to a
                        specific resource instance.
                      type: string
                    workspace:
                      description: Workspace optionally identifies the kcp workspace
                        that owns the target resource.
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
